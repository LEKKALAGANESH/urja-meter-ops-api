fastapi==0.139.2
uvicorn[standard]==0.38.0
httpx==0.28.1
pydantic==2.12.5
pydantic-settings==2.7.1

# Pinned explicitly, not left to transitive resolution: fastapi<=0.123 capped starlette
# below 0.51, which held it on a release carrying 7 known CVEs (PYSEC-2026-161/248/249/
# 2280/2281). Pinning here means a future fastapi bump cannot silently reintroduce them.
starlette==1.3.1
