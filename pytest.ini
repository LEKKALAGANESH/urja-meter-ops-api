[pytest]
testpaths = tests
asyncio_mode = auto
asyncio_default_fixture_loop_scope = function
markers =
    live: hits the real Urja portal; requires credentials. Deselected by default.
addopts = -q --strict-markers -m "not live"
filterwarnings =
    ignore::DeprecationWarning
